#pragma once

enum OperatorType
{
	POST_INC, POST_DEC, PRE_INC, PRE_DEC,
	UNARY_PLUS, UNARY_MINUS,
	LOGIC_NOT, BITWISE_NOT,

	MUL, DIV, MOD,
	ADD, SUB,

	LEFT_SHIFT, RIGHT_SHIFT,

	LESS, LESS_EQ,
	GREATER, GREATER_EQ,
	EQEQ, NOT_EQ,

	AND,
	XOR,
	OR,
	LOGIC_AND,
	LOGIC_OR,

	EQ,

	ADD_EQ, SUB_EQ, MUL_EQ, DIV_EQ, MOD_EQ,
	LEFT_SHIFT_EQ, RIGHT_SHIFT_EQ,
	AND_EQ, XOR_EQ, OR_EQ,

	MAX_TYPES,
};
